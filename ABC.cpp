#include <bits/stdc++.h>
using namespace std;
int main()
{
    freopen("ABC.INP", "r", stdin);
    freopen("ABC.OUT", "w", stdout);

    srand(time(nullptr));
    cout << rand() % 2;

    return 0;
}